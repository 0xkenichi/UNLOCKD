/**
 * Centralized API utility for Vestra Protocol.
 * Re-exporting from @/utils/api to resolve build dependency issues.
 */
import { api, fetchApi } from '@/utils/api';

export { api, fetchApi };
export default api;
