name: Identity connect prompt
steps:
  - goto: /identity
  - expectText: Global Identity
  - expectText: Link multiple wallets to a single Gitcoin Passport score. Optional verification unlocks better loan terms.
